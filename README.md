# Gemini AI Chatbot

A chatbot built using the Gemini API and Node.js.

## Installation

1. Clone the repository.
2. Run `npm install` to install dependencies.
3. Set up the `.env` file with your Gemini API key.

## Usage

1. Run the bot with `node main.js`.
2. Interact with the bot in the terminal.
